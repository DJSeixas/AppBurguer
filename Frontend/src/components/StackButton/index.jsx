import { Container, Text } from "./styles"


const StackButton = ({title}) => {
    return (
        <Container>
            <Text>{ title }</Text>
        </Container>
    )
}

export default StackButton